# Configure default values here for example
# email: admin@example.com
# domain: example.com
podman:
  socket_url: "unix:///var/run/podman/podman.sock"
  timeout: 30
  tls_verify: true
imap:
  server: "imap.example.com"
  port: 993
  username: "test-user"
  password: "test-password"
test_value: 28
