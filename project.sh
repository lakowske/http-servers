#!/bin/bash

# This script is no longer used to setup and run the project.  Instead
# run actions/build.py.  You can source this script to setup the bash
# environment for the project.
source .venv/bin/activate
export PYTHONPATH=$PYTHONPATH:$(pwd)

